fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
httpx==0.28.1
langdetect==1.0.9
cryptography==44.0.0
email-validator==2.2.0
python-dotenv==1.0.1
aiosmtplib==3.0.1
# v2.4 item 7 — scheduled (automatic) runs. APScheduler is used in-process
# inside FastAPI's lifespan; single-firing across the two Uvicorn workers is
# guarded by a Postgres advisory lock (see app/services/scheduler.py).
# tzdata ships the IANA zone database on systems (like slim Linux images)
# where the OS may not provide /usr/share/zoneinfo.
APScheduler==3.10.4
tzdata==2024.2
